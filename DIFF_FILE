diff --git a/CHANGELOG.md b/CHANGELOG.md
index 7865162..596f457 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -4,6 +4,7 @@
 
 - 修复明确结果写入后 CSV/TXT 输入文件行数不减少的问题：每条结果先同步提交 SQLite 和实时 CSV，再按完整原始行原子删除输入中的对应一条记录。
 - XLSX/XLSM 在处理过程中绝不删行；所有浏览器线程结束、SQLite/CSV 完成落盘后只打开并重建工作簿一次，批量删除全部明确结果行，同时保留其他工作表。
+- 用户点击停止时也会先等待全部线程结束并关闭写入线程，再对 XLSX/XLSM 执行本轮唯一一次批量删行；明确结果会删除，未完成行和 SQLite 断点继续保留。
 - 合法重复行按完成数量删除；CSV/TXT 实时重定位，XLSX/XLSM 结束后一次性重定位，不会因行号变化删错人。
 - 中途停止再启动时以 SQLite 保存的原始任务身份续跑，不会把缩短后的当前行号重复导入；启动时自动补删旧版已完成但仍留在输入文件的行。
 - 修复切换到新输入文件后，第一条耗时任务完成前 GUI 仍显示上一份输入文件统计和日志的问题。
diff --git a/RELEASE_NOTES_v1.1.2.md b/RELEASE_NOTES_v1.1.2.md
index 289485d..ae835e4 100644
--- a/RELEASE_NOTES_v1.1.2.md
+++ b/RELEASE_NOTES_v1.1.2.md
@@ -4,6 +4,7 @@
 
 - CSV/TXT 的每条明确结果先同步写入 SQLite 和实时结果 CSV，确认成功后立即从输入文件删除对应行。
 - XLSX/XLSM 处理过程中保持全部输入行不变；所有线程结束后只执行一次工作簿重建，批量删除有明确结果的行并保留其他工作表。
+- 点击停止后先等待所有线程退出，再执行同样的一次性 XLSX/XLSM 删除；日志不再笼统显示“未执行输入文件重建”。
 - 删除按完整原始行匹配，重复人员/重复首列值只按已完成数量删除。
 - 断点重启使用 SQLite 中保存的原始任务，不会因输入文件已经缩短而错位或重复导入；启动会补删此前已有明确结果的遗留输入行。
 - 选择新的输入文件并开始后，GUI 不再保留上一批的总数、完成数、CSV 数量和日志。
diff --git a/controller.py b/controller.py
index eda1537..5149f27 100644
--- a/controller.py
+++ b/controller.py
@@ -356,7 +356,21 @@ class AppController:
                 rebuilt_writer.close()
                 self._log(f"跨输入行强身份信号补充 {updated} 条结果，已从 SQLite 重建实时 CSV")
             if self.stop_event.is_set():
-                self._log("任务已停止；未执行输入文件重建，已保留数据库断点")
+                if self._defer_input_rewrite:
+                    # “停止”同样先等待全部 worker join 并关闭数据库写线程；此时才对
+                    # XLSX/XLSM 执行本轮唯一一次批量删行，未完成任务仍保留在断点库。
+                    stopped_removed = self.input_rewriter.remove_people(
+                        self.database.done_people(self.config.input_file)
+                    )
+                    self._log(
+                        f"任务已停止；全部线程已结束，已一次性重建 XLSX/XLSM 并删除 "
+                        f"{stopped_removed} 条明确结果；未完成行和数据库断点已保留"
+                    )
+                else:
+                    self._log(
+                        "任务已停止；CSV/TXT 的明确结果行已在提交后实时删除；"
+                        "未完成行和数据库断点已保留"
+                    )
                 self._emit_progress("已停止")
                 self._final_status = "已停止"
             else:
diff --git a/tests/test_controller_offline.py b/tests/test_controller_offline.py
index 78fcf18..d98110d 100644
--- a/tests/test_controller_offline.py
+++ b/tests/test_controller_offline.py
@@ -46,6 +46,17 @@ class FakeBrowserSession:
         self.page = object()
 
 
+class StoppingFakeBrowserSession(FakeBrowserSession):
+    def __init__(self, **kwargs: object) -> None:
+        super().__init__(**kwargs)
+        self.stop_event = kwargs["stop_event"]
+
+    def process(self, person: object) -> list[ProfileResult]:
+        results = super().process(person)
+        self.stop_event.set()
+        return results
+
+
 class ControllerOfflineTests(unittest.TestCase):
     def test_proxy_browser_connection_error_is_detected_for_refresh(self) -> None:
         self.assertTrue(_is_proxy_network_error("Page.goto: net::ERR_SOCKS_CONNECTION_FAILED"))
@@ -108,6 +119,33 @@ class ControllerOfflineTests(unittest.TestCase):
             finally:
                 workbook.close()
 
+    def test_stopped_xlsx_waits_for_worker_then_bulk_deletes_completed_rows(self) -> None:
+        with tempfile.TemporaryDirectory() as directory:
+            root = Path(directory)
+            source = root / "people.xlsx"
+            workbook = openpyxl.Workbook()
+            workbook.active.append(["first_name", "last_name", "note"])
+            workbook.active.append(["Jane", "Doe", "done"])
+            workbook.active.append(["John", "Doe", "pending"])
+            workbook.create_sheet("keep").append(["other sheet", "must stay"])
+            workbook.save(source)
+            workbook.close()
+            controller = AppController(RunConfig(source, root / "output", thread_count=1, browser_mode="无头"))
+            StoppingFakeBrowserSession.observed_input_counts = []
+            with patch("controller.BrowserSession", StoppingFakeBrowserSession):
+                controller.run()
+            self.assertEqual(StoppingFakeBrowserSession.observed_input_counts, [2])
+            self.assertEqual(controller._final_status, "已停止")
+            workbook = openpyxl.load_workbook(source, read_only=True, data_only=True)
+            try:
+                self.assertEqual(
+                    list(workbook.active.iter_rows(values_only=True)),
+                    [("first_name", "last_name", "note"), ("John", "Doe", "pending")],
+                )
+                self.assertEqual(list(workbook["keep"].iter_rows(values_only=True)), [("other sheet", "must stay")])
+            finally:
+                workbook.close()
+
     def test_proxy_thread_limit_validation(self) -> None:
         with tempfile.TemporaryDirectory() as directory:
             root = Path(directory)
