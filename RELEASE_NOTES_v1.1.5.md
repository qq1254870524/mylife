# MyLife v1.1.5

发布日期：2026-08-17

## 新增

- GUI 增加 `HTTP接口` 查询方式，原 `浏览器` 查询方式完整保留。
- 新模块 `http_worker.py`：每个 worker 用浏览器取得 Cloudflare 会话后，将 Cookie、User-Agent 和代理出口同步到独立 HTTP 会话。
- 搜索页、分页和详情页优先通过 HTTP 查询，继续复用现有解析、候选匹配、SQLite、CSV 和输入重建流程。

## 稳定性

- HTTP 会话遇到 401/403/429 或 Cloudflare 页面时自动重新初始化并重试。
- HTTP 仍不兼容时使用同会话浏览器文档兜底；代理通道异常沿用无损重排队和出口恢复逻辑。
- 修复点击“停止”时后台日志线程与 Tk 主线程争用日志锁造成的 GUI 未响应。

## 验证

- 本地真实链路验证：浏览器初始化取得 9 个 Cookie，HTTP 搜索响应 200、100 个候选，详情页由 HTTP 成功解析。
- 新增 HTTP 会话、代理、Cookie、重试、停止和控制器选择测试。
- 完整离线测试 95 项通过。
