# MyLife 正式版 v1.1.4

## 停止流程修复

- 用户点击“停止”后，当前正在导航、采集详情或处理 Cloudflare 的任务统一保存为 `retry`。
- 底层返回 `RuntimeError: cancelled` 时不再误标为终态 `failed`。
- 停止期间不再为包装后的取消执行诊断截图或浏览器重建，缩短线程收尾时间。
- 详情已取得但返回搜索列表恰逢停止时同样略过诊断截图，正常取消页不再进入故障目录。
- GUI 的“开始”按钮会在 profiles 和临时缓存清理完成后才重新启用，大批浏览器退出较慢时不会提前启动下一轮。
- Chromium 留下仅 AppContainer 可修改的空缓存目录时，程序会恢复当前用户 ACL 后再次删除，保证重新启动使用全新 profiles。
- 已完成结果仍会先写入 SQLite/CSV；全部线程退出后，XLSX/XLSM 仍只重建一次并删除明确完成行。
- 未完成行、代理故障行和取消行全部保留，可按原配置继续断点运行。
- 队列暂时为空但仍有浏览器正在处理时，其余 worker 会保持等待；该任务若因代理或 Cloudflare 重新入队，仍由完整线程池继续处理，不再出现四线程逐步变成三、二、一个线程。
- 停止/异常中断会退回本次 `running` 增加的尝试次数；启动时自动释放旧版遗留的达到尝试上限却仍为 `pending/retry` 的行，所有保留输入均可继续领取。
- 完整成功后即使 SQLite 断点已经清理，GUI 仍保留真实完成数，不再显示“已完成但完成 0”；CSV 和字段统计继续保留最终值。

## 无表头 XLSX 自适应

- 自动识别电话、SSN、已有生日、姓名、年龄、地址、城市、州、邮编、邮箱、电话列表、出生年、参考年龄的无表头人员表。
- 第一物理行完整保留为任务；明确结果在全部线程结束后可按真实物理行一次性删除，不新增伪表头。
- 4 位数字邮编自动补前导零；姓名+城市州邮编搜索、姓名兜底搜索及电话/邮箱/地址/年龄匹配信号全部可用。

## 代理瞬时网络恢复

- 实跑发现的 `ERR_SSL_PROTOCOL_ERROR` 和 `ERR_TIMED_OUT` 不再消耗人员任务的业务重试次数。
- 程序会关闭错误页、等待专属代理恢复并用全新 profile 继续；四个工作线程不会因这两类短暂网络抖动永久减少。

## 人口资料完整度

- MyLife 身份匹配成功但生日未公开时，可从输入原始列中的完整生日补入，备注明确记录“输入资料完整日期”，并由该日期确定星座。
- 只有完整且可解析的年月日才会使用；只有年份或月日为 `00` 的部分日期继续保持为空，避免制造并不存在的唯一生日。
- 姓名+地点与姓名搜索都无结果时，备注仍保留真实未命中原因；若输入本身有完整生日，只补生日/星座并单独标明来源，不改变“无结果”结论。
